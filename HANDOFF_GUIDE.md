# 📋 HANDOFF GUIDE - Передача проекта на двоих разработчиков

**Дата:** 2026-09-02  
**Статус:** ✅ Готово к работе  
**Целевая аудитория:** Оператор 1 (Воины) и Оператор 2 (Маги)

---

## 🎯 ТРИ ГЛАВНЫХ ДОКУМЕНТА (читайте в таком порядке)

### 1️⃣ [ARCHITECTURE_DUAL_SYSTEM.md](./ARCHITECTURE_DUAL_SYSTEM.md) - ОБЯЗАТЕЛЬНО! ⚠️

**Это основа всего.** Здесь объяснено:
- ✅ Как работает ЕДИНАЯ БД (таблица `characters` для воинов И магов)
- ✅ Как работает ЕДИНАЯ валюта (медяки видны обоим персонажам)
- ✅ Как разделен код между двумя разработчиками
- ✅ Почему клиент - это визуализация, а источник истины - сервер

**Время прочтения:** 20 минут  
**Важность:** КРИТИЧЕСКАЯ

### 2️⃣ [MAGE_DEVELOPER_GUIDE.md](./MAGE_DEVELOPER_GUIDE.md) - Для ОП2

Полная спецификация системы магии:
- Характеристики магов (Мудрость, Духовность, Выносливость)
- Система элементов (Земля, Вода, Огонь, Воздух)
- Структура заклинаний
- БД для магов
- API endpoints для магии
- План разработки (5 фаз)

**Время прочтения:** 30 минут  
**Важность:** ВЫСОКАЯ для ОП2

### 3️⃣ [CONTRIBUTING.md](./CONTRIBUTING.md) - Правила разработки

Git workflow, code standards, архитектура, testing.

**Время прочтения:** 15 минут  
**Важность:** СРЕДНЯЯ

---

## 👨‍💻 Что делает каждый разработчик?

### Оператор 1 (ДА) - Система Воинов ⚔️

**Что вы делали и будете делать:**
- ✅ Система персонажей-воинов (Warrior class)
- ✅ Боевая система пошагового боя (duel_scene.py)
- ✅ Система валюты (медяки/серебро/золото) - ОБЩАЯ!
- ✅ Магазин напитков
- ✅ Инвентарь
- ✅ Таблица статистики боя

**Ваши файлы (не трогайте это второго разработчика):**
```
scenes/duel_scene.py
core/character/warrior.py
core/stats/warrior_stats.py
ui/character_card.py
ui/battle_ui.py
```

**Адрес ветки:** `feature/warrior-*`

---

### Оператор 2 (ОП2) - Система Магии 🧙

**Что вам нужно создать:**
- 🧙 Система персонажей-магов (Mage class)
- 🧙 Система магии и заклинаний
- 🧙 Система элементов (4 типа)
- 🧙 Боевая система для магов (отдельная сцена)
- 🧙 Таблица статистики магических боев
- 🧙 AI для магических ботов

**Ваши файлы (создавайте их, не трогайте warrior):**
```
scenes/mage_duel_scene.py
core/character/mage.py
core/magic/spell.py
core/magic/element.py
core/magic/spellbook.py
ui/mage_card.py
ui/mage_battle_ui.py
```

**Адрес ветки:** `feature/mage-*`

---

## 💰 ЗОЛОТОЕ ПРАВИЛО: ЕДИНАЯ ВАЛЮТА

### Пример 1: Воин побеждает

```
Начальное состояние:
- Воин (character_id=1): 50 медяков
- Маг (character_id=2):  30 медяков

Воин побеждает → получает 10 медяков:

После (в БД):
- Воин (character_id=1): 60 медяков  ← обновилось
- Маг (character_id=2):  30 медяков   ← не изменилось
```

### Пример 2: Маг покупает зелье

```
Начальное состояние:
- Воин (character_id=1): 60 медяков
- Маг (character_id=2):  30 медяков

Маг покупает зелье за 20 медяков:

api.subtract_currency(character_id=2, copper=20)

После (в БД):
- Воин (character_id=1): 60 медяков    ← не изменилось
- Маг (character_id=2):  10 медяков    ← обновилось!
```

### Пример 3: Конвертация

```
Начальное состояние:
- Воин: 100 медяков

Воин побеждает → получает 100 медяков:

add_currency(character_id=1, copper=100)

Автоматическая конвертация:
- 100 медяков → 1 серебро

После (в БД):
- Воин: 0 медяков + 1 серебро (вместо 200 медяков!)
```

---

## 🚫 ОПАСНЫЕ ОШИБКИ (НЕ ДЕЛАЙТЕ!)

### ❌ Ошибка 1: Считать деньги на клиенте

```python
# ❌ НЕПРАВИЛЬНО:
class ShopUI:
    def buy_item(self):
        self.player_copper -= 20  # Только на клиенте!
        # На сервере ничего не изменилось!
        # Следующий refresh покажет старое значение!

# ✅ ПРАВИЛЬНО:
class ShopUI:
    def buy_item(self):
        response = api.shop_buy(character_id=1, item_id="ale")
        if response.success:
            self.copper = response.copper  # Берем из сервера
        else:
            show_error("Недостаточно средств!")
```

### ❌ Ошибка 2: Создать отдельную валюту для магов

```python
# ❌ НЕПРАВИЛЬНО (ОП2, не делайте этого!):
class Mage:
    def __init__(self):
        self.mage_copper = 50     # отдельная валюта!
        self.mage_silver = 0      # отдельная валюта!
        self.mage_gold = 0        # отдельная валюта!

# ✅ ПРАВИЛЬНО:
class Mage(BaseCharacter):
    def __init__(self, data):
        self.id = data['id']
        # Читаем валюту из одной БД, как и воины
        # self.copper = data['copper']  # из characters.copper
```

### ❌ Ошибка 3: Не проверить хватает ли денег

```python
# ❌ НЕПРАВИЛЬНО:
def buy_spell_tome(mage_id, cost):
    subtract_currency(mage_id, copper=cost)  # Может быть недостаточно!

# ✅ ПРАВИЛЬНО:
def buy_spell_tome(mage_id, cost):
    if subtract_currency(mage_id, copper=cost):  # Проверяем успех
        learn_spell(mage_id, spell_id)
    else:
        return {"error": "Недостаточно средств"}
```

---

## 📝 Что можно менять? Что нельзя?

### Таблица разрешений

| Файл/Система | ОП1 (Воины) | ОП2 (Маги) | Оба |
|---|---|---|---|
| `warrior.py` | ✅ Хозяин | ❌ Нельзя | ⚠️ Только критические баги |
| `mage.py` | ❌ Нельзя | ✅ Хозяин | ⚠️ Только критические баги |
| `duel_scene.py` | ✅ Хозяин | ❌ Нельзя | ⚠️ Только критические баги |
| `mage_duel_scene.py` | ❌ Нельзя | ✅ Хозяин | ⚠️ Только критические баги |
| `magic/*` | ❌ Нельзя | ✅ Хозяин | - |
| `database.py` | ⚠️ Добавляй методы | ⚠️ Добавляй методы | ✅ Обсудить перед merge |
| `main.py` (сервер) | ⚠️ Добавляй endpoints | ⚠️ Добавляй endpoints | ✅ Обсудить перед merge |
| `shop.py` | ✅ Лидер | 📎 Может адаптировать | ✅ Вместе |
| `inventory.py` | ✅ Лидер | 📎 Может адаптировать | ✅ Вместе |
| `character_select.py` | ✅ Добавляет выбор профессии | 📎 Может адаптировать | ✅ Вместе |

---

## 🔄 Git Workflow

### Для обоих разработчиков:

```bash
# 1. Создать ветку
git checkout -b feature/ваша-фича

# 2. Регулярно коммитить
git add -A
git commit -m "Добавить функцию X"

# 3. Перед merge - обновить main
git pull origin main

# 4. Если конфликты - разрешить их
git merge main

# 5. Push в свою ветку
git push origin feature/ваша-фича

# 6. На GitHub создать Pull Request
# Другой разработчик делает code review
# Если ОК - merge в main
```

### Правила для коммитов:

```
✅ ПРАВИЛЬНО:
  "Add mage spell system - implement earth element spells"
  "Fix warrior damage calculation bug"
  "Add mana regeneration to mage class"

❌ НЕПРАВИЛЬНО:
  "fix"
  "update"
  "Changes"
  "sdfjkl"
```

---

## 🧪 Как тестировать

### Для ОП1 (Воины):
```bash
# Запустить тесты воинов
pytest tests/test_warrior.py -v
pytest tests/test_duel_scene.py -v
pytest tests/test_currency.py -v
```

### Для ОП2 (Маги):
```bash
# Запустить тесты магов (когда создадите)
pytest tests/test_mage.py -v
pytest tests/test_spells.py -v
pytest tests/test_magic_duel.py -v
```

### Оба:
```bash
# Интеграционные тесты (вместе)
pytest tests/test_integration.py -v

# Все тесты
pytest -v
```

---

## 💬 Коммуникация между разработчиками

### Где синхронизировать:

1. **Каждый день** - 5 минут обсуждение что сделал, что буду делать
2. **Перед merge** - обязательный code review друг друга
3. **Если конфликт** - обсудить как решить (не push без согласия)
4. **Новый endpoint/метод БД** - обсудить в чате ДО реализации

### Вопросы перед тем как начать (для ОП2):

1. "Какие методы БД я должен использовать для валюты?" → Смотрите `database.py`
2. "Как магам купить тома заклинаний?" → Используйте `shop_buy` API
3. "Может ли маг и воин быть в одном бою?" → Нет, разные сцены
4. "Что если 100+ медяков?" → Автоконвертируется в серебро

---

## 📚 Дополнительные ресурсы

- **README.md** - Общее описание проекта
- **PROJECT_STATUS.md** - Метрики и прогресс
- **server/database.py** - Посмотрите доступные методы
- **server/main.py** - Посмотрите текущие endpoints

---

## ✅ Чек-лист перед началом работы

### Для ОП1:
- [ ] Прочитал ARCHITECTURE_DUAL_SYSTEM.md
- [ ] Прочитал CONTRIBUTING.md
- [ ] Знаю что могу / не могу менять
- [ ] Понимаю что валюта ОБЩАЯ
- [ ] Готов к code review ОП2

### Для ОП2:
- [ ] Прочитал ARCHITECTURE_DUAL_SYSTEM.md ← ОБЯЗАТЕЛЬНО!
- [ ] Прочитал MAGE_DEVELOPER_GUIDE.md
- [ ] Прочитал CONTRIBUTING.md
- [ ] Понимаю что магов и воинов одна БД
- [ ] Понимаю что валюта ОБЩАЯ для всех персонажей
- [ ] Не буду создавать отдельную валюту
- [ ] Буду использовать методы БД для работы с деньгами
- [ ] Готов к code review ОП1

---

## 🎯 Ближайшие шаги

### Для ОП1:
1. Продолжите текущую работу
2. Ждите pull requests от ОП2
3. Делайте code review добросовестно
4. Помогайте ОП2 если что-то не ясно

### Для ОП2:
1. **Сегодня:** Прочитайте ARCHITECTURE_DUAL_SYSTEM.md (20 минут)
2. **Завтра:** Прочитайте MAGE_DEVELOPER_GUIDE.md (30 минут)
3. **День 3:** Создайте ветку `feature/mage-foundation`
4. **День 3-5:** Создайте класс `Mage` в `core/character/mage.py`
5. **День 5+:** Система магии и заклинаний

---

## 🆘 Если что-то не ясно

**Вопросы ОП2 к ОП1:**
- "Как добавить новый endpoint?" → Смотри `server/main.py` примеры
- "Где хранить данные мага?" → В таблице `characters` в `profession_data_json`
- "Как работает конвертация денег?" → Смотри `ARCHITECTURE_DUAL_SYSTEM.md` раздел "Валюта"

**Вопросы ОП1 к ОП2:**
- "Могу ли я менять систему магии?" → Нет, это область ОП2
- "Что делать если маг использует наше API?" → Скажите ОП2 что нужно

---

**Готово к запуску!** 🚀

**Последний шаг:** ОП2 пишет тест что маг и воин видят одинаковую валюту.

---

**Документ создан:** 2026-09-02  
**Статус:** ✅ Готов к передаче обоим разработчикам  
**Дальше:** Начните разработку согласно плану!
